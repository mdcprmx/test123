#include <stdio.h>

int main() {
    
    // Section 1 - Declarization

    int a, b, c, rezult;
    
    // Section 2 - Programm body

    scanf("\n %d %d %d", &a, &b, &c);    // awaits for 3 user inputs
    rezult = a + b + c;                  // then does whatever mathematics listed
    printf("resultato: %d\n", rezult);   // and shows result.


    return 0;
}